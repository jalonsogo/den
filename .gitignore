node_modules/
out/
dist/
dist-electron/
*.log
.DS_Store
.claude/settings.local.json

# TypeScript incremental build info
*.tsbuildinfo

# Playwright MCP output (screenshots, traces) — transient tool artifacts
.playwright-mcp/

# Raw icon exports from Icon Composer — transient, tool-named renders.
# Source of truth is resources/icon/den.icon; the committed, code-referenced
# icons are resources/icon/dock.png + dock-dark.png (see `npm run sync:icons`).
resources/icon/den-*-1024@*.png
